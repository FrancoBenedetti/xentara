# User Observations, issues and new features required

## New features

1. Add a copy function accessed via an icon next to the editable fields in the pubish modal aceesible from the [[Publish] button in the srticle cards in the /dashboard/hubs/[slug] form]. The copy function should copy the content of the field to the clipboard and display a small message indicating that the content has been copied to the clipboard. The copy message string should be preceded by and joined to by an instruction message string which is to be stored in a variable for the hub. The default value of the instrauction message should be "Translate etc this message into good {language}:" Where the language is the default language for the hub. The icon should be a copy icon. 
2.