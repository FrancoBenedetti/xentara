# User Observations, issues and new features required

1. The publications page should allow the user to select between any of the hubs to view th publications.
2. The publications page should have filters to find items that fall within a data range, match a taxonomy term (flavor), contain a search term. Use an infinite scroll technique so that not all the pages are loaded at once, and allow the sort order to be toggles between newest to oldest (default) and oldest to newest. The search and filters should persist when the user navigates away from the page and returns.
3. How do we get to the promotions manager page? This seems to be hidden. It does not appear to be accessible from the settings pages. Likely, the quickest access is from the left panel under the 'hub' section?
